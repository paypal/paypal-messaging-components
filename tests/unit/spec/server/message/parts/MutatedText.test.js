describe('<MutatedText />', () => {
    test.todo('handles tag string or options object');
    test.todo('handles multiple tags');
    test.todo('handles br mutation');
    test.todo('handles replace mutation');
});
